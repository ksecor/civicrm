     <div class="form-item">
	     <span class="horizontal-position"> 
              {foreach from=$AToZBar item=row }
                {$row}&nbsp;|&nbsp; 
              {/foreach}
            </span>
     </div>
